📁 PROJECT STRUCTURE - Complete Overview
═══════════════════════════════════════════════════════════════════════════════

my_fb2k_extension/
│
├── 📄 README.md ⭐ START HERE
│   └── Feature overview, build steps, known limitations
│
├── 🔨 CMakeLists.txt                ← ⭐ Actual build system in use (CMake)
│
├── 📄 Compile_commands.md          ← ⚠️ Leftover from earlier scaffolding, not
│                                       kept in sync with the current CMake
│                                       setup — treat CMakeLists.txt as the
│                                       source of truth, not this file
│
├── src/                            (Source Code)
│   ├── stdafx.h                    ← Precompiled header (includes SDK headers)
│   ├── stdafx.cpp                  ← PCH implementation (empty, compiled with /Yc)
│   ├── dllmain.cpp                 ← DLL entry point
│   ├── extension.cpp               ← DECLARE_COMPONENT_VERSION (component metadata only)
│   ├── history_manager.h           ← Playback history data model (no UI code)
│   ├── history_manager.cpp         ← play_callback hook, history storage, play/back logic
│   ├── history_panel.h             ← ui_element / ui_element_instance (the panel UI)
│   └── history_panel.cpp           ← Panel window, controls, font/color theming
│
├── sdk/                            (foobar2000 SDK - external, not authored by us)
│   ├── pfc/                        ← Utility lib. Has pfc.vcxproj -> pfc.lib
│   ├── libPPUI/                    ← UI helpers (present, not currently used)
│   └── foobar2000/
│       ├── SDK/                    ← Core interfaces. Has foobar2000_SDK.vcxproj -> foobar2000_SDK.lib
│       ├── shared/                 ← Has shared.vcxproj -> shared.dll + shared.lib
│       ├── helpers/                ← Optional helper .cpp (not currently used)
│       └── foobar2000_component_client/
│           └── component_client.cpp   ← No .vcxproj - compiled directly into our SOURCES
│
├── build/                          (Output - x64, auto-generated by CMake)
│   └── bin/Release/
│       └── foo_previous.dll        ← Final product (64-bit) 🎉
│
└── build-x86/                      (Output - Win32, auto-generated by CMake)
    └── bin/Release/
        └── foo_previous.dll        ← Final product (32-bit) 🎉


📄 README.md
   ✓ What the component does (Playback History panel)
   ✓ Build instructions (x64 + x86)
   ✓ Known limitations / open items
   ✓ File-by-file structure notes

🔨 CMakeLists.txt
   ✓ CMake build system (Visual Studio 17 2022 generator, MSVC v143 toolset)
   ✓ Builds both x64 (`-A x64`) and Win32 (`-A Win32`) from separate build dirs
   ✓ Links pfc / shared / foobar2000_SDK via include_external_msproject
   ✓ Sets output filename to foo_previous.dll via OUTPUT_NAME

📄 Compile_commands.md  ⚠️ stale / not authoritative
   ✓ Leftover reference doc from earlier scaffolding
   ✓ Not regenerated alongside CMakeLists.txt changes — may list flags/paths
     that no longer match the real build
   ✓ Defer to CMakeLists.txt + README.md instead

📄 Makefile  ⚠️ unused
   ✓ Leftover MinGW/GNU Make path from earlier scaffolding
   ✓ Never used in this project's actual build/debug history — everything
     was built via `cmake` + MSVC
   ✓ Safe to delete unless you specifically want a MinGW build path

📄 my_fb2k_extension.vcxproj.bak  ⚠️ safe to delete
   ✓ Backup taken during manual .vcxproj edits early in development
   ✓ Those fixes now live in CMakeLists.txt instead (see README.md's "golden
     rule": don't hand-edit the generated .vcxproj — it's overwritten on
     every `cmake -B build`)

.vscode/  ⚠️ optional
   ✓ Leftover editor config from earlier scaffolding (tasks.json /
     c_cpp_properties.json / settings.json, if present)
   ✓ Not required — build/debug this project via `cmake`/`cmake --build`
     directly, or open the folder in VS Code with the CMake Tools extension

src/stdafx.h / stdafx.cpp
   ✓ Precompiled header pair
   ✓ Pulls in Windows headers + foobar2000 SDK headers
   ✓ Only extension.cpp / dllmain.cpp compile against the PCH (/Yu)
   ✓ history_manager.cpp / history_panel.cpp also use /Yu (see CMakeLists.txt)

src/dllmain.cpp
   ✓ DLL entry point
   ✓ Minimal implementation, doesn't need editing

src/extension.cpp
   ✓ DECLARE_COMPONENT_VERSION only (name / version / description shown in
     foobar2000 → Preferences → Components)
   ✓ No menu command code here anymore — the "Playback History" menu entry
     is auto-generated by history_panel.cpp's ui_element_v2 popup flag

src/history_manager.h / history_manager.cpp
   ✓ Hooks play_callback_static to track the last 20 played tracks
   ✓ Owns all playback/navigation logic: play a track from history, "Back"
     (removes current track from history, plays the previous one)
   ✓ Checks the active playlist for an existing copy of a track before
     inserting a duplicate (falls back to appending if not found)
   ✓ No UI code — calls history_ui_element_instance::refresh_all() to push
     updates out to whatever panel instance(s) are currently open

src/history_panel.h / history_panel.cpp
   ✓ ui_element (entrypoint) + ui_element_instance (actual panel) pair
   ✓ Registered with KFlagHavePopupCommand → foobar2000 auto-generates the
     "Playback History" menu command (opens as a floating popup, or can be
     docked into a layout via Layout → Add New UI Element instead)
   ✓ ListBox (track list, double-click to play) + Back button
   ✓ Font pulled live from foobar2000's own list-font setting
     (Preferences → Display → Fonts) via query_font_ex()
   ✓ Background/text colors pulled live from the active theme (incl. dark
     mode) via query_std_color(), with real-time updates on theme change
   ✓ Supports multiple simultaneously open instances (static registry)


🚧 Known open items (see README.md for details)
   • Back/click-to-play can insert into the wrong playlist if the track
     isn't in the currently active one
   • Panel colors don't yet cover every control (push button background in
     particular is limited by native Windows theming)
   • No Preferences → Tools page yet for user-adjustable settings